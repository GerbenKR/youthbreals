{% set currentSiteGroup = craft.app.sites.getCurrentSite().group %}
{% set cities = craft.app.sites.getSitesByGroupId(currentSiteGroup.id) %}

{% set clubOverview = craft.entries().section('clubsOverview').one() %}
{% set uitgaanOverview = craft.entries().section('uitgaanOverview').one() %}
{% set blogOverview = craft.entries().section('blogOverview').one() %}


<footer class="container py-4 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-10">
    <div>
        <h3 class="text-lg font-semibold">Voorbereid op Jongerenvakantie</h3>
        <p>© 2024 Voorbereid op Jongerenvakantie.</p>
        <p><i>Deze website maakt deels gebruik van beeldmateriaal van derden.</i></p>
        <p>info@voorbereid-op-vakantie.nl</p>
    </div>
    <div>
        <h4 class="font-bold">Bestemmingen</h4>
        <ul class="mt-3">
            {% for city in cities %}
                {% set globalSet = craft.globalSets().handle('site').siteId(city.id).one() %}
                <li>
                    <a href="{{ city.baseUrl }}" class="hover:underline">{{ globalSet.siteName }}</a>
                </li>
            {% endfor %}
        </ul>
    </div>
    <div>
        <h4 class="font-bold">Pagina's</h4>
        <ul class="mt-3">
            <li>
                <a href="{{ uitgaanOverview.url }}" class="hover:underline">Uitgaan in {{ site.siteName }}</a>
            </li>
            <li>
                <a href="{{ clubOverview.url }}" class="hover:underline">Clubs in {{ site.siteName }}</a>
            </li>
            <li>
                <a href="{{ blogOverview.url }}" class="hover:underline">Blogs over {{ site.siteName }}</a>
            </li>
        </ul>
    </div>
</footer>